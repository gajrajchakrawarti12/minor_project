spring.application.name=backend

# Server configuration
server.port=8081

# Database configuration
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:minor_project}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:Gajraj@0905}

# JPA configuration
spring.jpa.hibernate.ddl-auto=update

# JWT secret
jwt.secret=${JWT_SECRET:115548+64684dfakjgkj.bgghlaegbregheb.g.bflkab;bgajkf aj,gkjdfg}

